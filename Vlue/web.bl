use web;
use debug;
use base;


log("Service started.");

var page = import test;

var style = Style();
var script = Script();

$route('/');
fn index(){
    return template(page);
}

var web = Web();
web.host = 'localhost';
web.port = 8000;
web.start();

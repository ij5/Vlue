use web;
use base;


log("Service started.");

var page = import test;

var style = Style(page);

style.setColor('h1', 'red');
page = style.apply();

log(page);

$route('/');
fn index(){
    return page;
}

var web = Web();
web.host = 'localhost';
web.port = 8000;
web.start();
